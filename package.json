{
  "name": "civitaiuk-board",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public",
    "db:migrate:local": "wrangler d1 migrations apply civitaiuk --local",
    "db:migrate:remote": "wrangler d1 migrations apply civitaiuk --remote"
  },
  "devDependencies": {
    "wrangler": "^4.55.0"
  }
}